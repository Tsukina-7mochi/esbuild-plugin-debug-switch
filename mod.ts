// This is a fake file to make type completion work

/** A switch represents whether the debug is enabled in the build. */
export const isDebug = false as boolean;

/** Variables passed from the plugin. */
export const env: Record<string, unknown> = {};

throw Error('This file should not be imported');
